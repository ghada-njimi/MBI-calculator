# MBI-calculator

Projet Python pour calculer l'Indice de Masse Corporelle (IMC).

## Structure
- `bmi_calculator/` : package principal
- `main.py` : point d'entrée
- `tests/` : tests unitaires

## Installation
```bash
git clone <repo_url>
cd bmi_calculator
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt